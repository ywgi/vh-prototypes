import Vehicle from "@/types/VehicleType";

export default function SideMenu({vehicles}: {vehicles: Vehicle[]}) {
    return(
        <div className="hidden md:flex md:flex-col w-70 bg-pink-200 ">

        </div>
    );
}